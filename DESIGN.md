## Design:

Parameters:
Tokenizer: GPT-2 from transformer library's autotokeniezr
model specifications: same as GPT-2

Breakdown:

Need a folder for model architecture
Need a folder for training/data processing
