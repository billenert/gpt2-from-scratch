# Done:
Implemented Transformer Architecture (needs testing)

# To-Do:
Decide on a dataset to use for training
Figure out tokenizer (probably just going to use GPT-2, too much of a main to write my own tokenizer)
Write a data/training pipeline.